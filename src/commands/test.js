

module.exports = {
	name: 'test',
	description: 'use to test out code.',
	async execute(message) {
		// await createCache("abc", "xyz");
		// await createCache("mno", "efg");
		// const obj = await cacheExists("mno");
		// const obj = await deleteCache("xyz");
		// const obj = await getCacheData();
		// console.log(obj);
	},
};